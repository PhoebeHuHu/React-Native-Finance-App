export interface ExpenseType {
    id: string;
    name: string;
    amount: string;
    percentage: string;
}

export interface IncomeType {
    id: string;
    name: string;
    amount: string
}
export interface Spending {
    id: string;
    name: string;
    amount: string;
    date: string;
}